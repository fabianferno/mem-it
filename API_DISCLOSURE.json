{
  "project": "mem-it",
  "schema": "qvac-hackathon.api-disclosure.v1",
  "summary": "All AI inference (STT, LLM completion, embeddings, RAG, and multimodal image analysis) runs fully on-device via @qvac/sdk. No user data — audio, transcripts, embeddings, graph, attached photos, or derived content — ever leaves the device. There are zero cloud AI/inference API calls; image understanding uses an on-device vision-language model, not a cloud vision API.",
  "ai_inference_remote_calls": [],
  "remote_calls": [
    {
      "name": "QVAC model weight download (Hugging Face registry)",
      "purpose": "One-time download of model weights on first launch, performed by the @qvac/sdk loader from the model sources declared in src/qvac/models.ts (registry://hf/...). Weights are cached locally; subsequent runs are fully offline.",
      "performed_by": "@qvac/sdk",
      "direction": "download only (static model files)",
      "user_data_sent": "none",
      "endpoints": [
        "registry://hf/ggerganov/whisper.cpp/.../ggml-small-q8_0.bin (Whisper small Q8_0, STT)",
        "registry://hf/unsloth/Llama-3.2-1B-Instruct-GGUF/.../Llama-3.2-1B-Instruct-Q4_0.gguf (Llama 3.2 1B, LLM)",
        "registry://hf/ChristianAzinn/gte-large-gguf/.../gte-large_fp16.gguf (GTE-large FP16, embeddings)",
        "registry://hf/Qwen/Qwen3-VL-2B-Instruct-GGUF/resolve/main/Qwen3VL-2B-Instruct-Q4_K_M.gguf (Qwen3-VL 2B, on-device multimodal image analysis)",
        "registry://hf/Qwen/Qwen3-VL-2B-Instruct-GGUF/resolve/main/mmproj-Qwen3VL-2B-Instruct-Q8_0.gguf (Qwen3-VL 2B vision projector / mmproj)"
      ],
      "frequency": "once per model (first use / cache miss); the vision model + projector download lazily the first time a photo is analyzed",
      "required_for_inference": false,
      "notes": "Models can be pre-provisioned for a fully offline demo. No AI computation happens over the network; only static weight files are fetched."
    }
  ],
  "non_ai_cloud_services": [],
  "peer_to_peer_sharing": {
    "name": "AirDrop / iOS share sheet (.memit bundles)",
    "purpose": "Optional device-to-device sharing of a single meeting bundle (transcript, summary, action items, chunk embeddings, graph subgraph) via expo-sharing / expo-document-picker. Uses Apple AirDrop (Bluetooth + direct Wi-Fi) — no server, no internet, no cloud.",
    "user_data_sent": "only the meeting the user explicitly chooses to share, and only to the device they select",
    "endpoints": [],
    "notes": "No remote API. Transport is peer-to-peer over the OS share sheet."
  },
  "device_permissions": [
    {
      "name": "Camera / Photo Library (iOS)",
      "purpose": "Attach a photo (of paper notes, a whiteboard, a place, or people) to a meeting; the image is analyzed on-device by the Qwen3-VL vision model and the description is stored locally with that meeting. Declared via NSCameraUsageDescription / NSPhotoLibraryUsageDescription.",
      "user_data_sent": "none — images and their analysis stay on the device and are never uploaded",
      "endpoints": []
    }
  ],
  "telemetry_or_analytics": "none added by mem-it. Standard Expo / React Native runtime may perform framework-level network activity outside the app's control; the app itself adds no analytics, crash reporting, or tracking.",
  "notes": "Storage (expo-sqlite) and file access (expo-file-system) are entirely local to the device."
}
